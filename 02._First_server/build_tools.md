Hvilke build tools findes der:
maven, gradle, ant, pip, NuGet, Rake, CMake

Maven           NPM
=============================
pom.xml         package.json

meta            meta
dependencies    dependencies
life cycles     scripts


Semantic versioning i package.json, læs op på det.
Package.lock.json er en fil der ?? <- kommer vi ikke til at snakke om